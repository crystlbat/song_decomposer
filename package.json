{
  "name": "tapline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tapline — the Netlify deploy's account backend. The app itself is still one static index.html.",
  "dependencies": {
    "@netlify/blobs": "^10.7.10"
  }
}
